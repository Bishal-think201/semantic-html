import { Box, Container } from "@mui/material";

const Main = () => {
  return (
    <Box component={"main"}>
      <Container maxWidth={false}>Main</Container>
    </Box>
  );
};
export default Main;
