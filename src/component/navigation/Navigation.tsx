import { Box, Container } from "@mui/material";

const Navigation = () => {
  return (
    <Box component={"nav"}>
      <Container maxWidth={false}>Navigation</Container>
    </Box>
  );
};
export default Navigation;
